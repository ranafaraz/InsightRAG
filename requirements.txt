# Full local stack: `pip install -r requirements.txt`
# For the minimal / CI stack use: `pip install -e ".[dev]"`
# (the offline deterministic backends need no model downloads or API keys).

# --- core ---
pydantic>=2.6
pydantic-settings>=2.2
rank-bm25>=0.2.2
numpy>=1.26
pypdf>=4.2
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9

# --- local ML backends (free, offline) ---
chromadb>=0.5
sentence-transformers>=2.7

# --- demo UI ---
streamlit>=1.34

# --- optional hosted provider (set LLM_BACKEND=openai) ---
# openai>=1.30

# --- optional RAGAS evals ---
# ragas>=0.1.9
# datasets>=2.19
